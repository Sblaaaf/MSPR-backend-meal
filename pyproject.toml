[tool.ruff.lint]
ignore = [
    "B008",   # FastAPI idiom: Depends()/Query()/File() as argument defaults is required.
    "DTZ005", # Project uses naive local datetimes throughout (DB columns are naive too).
    "DTZ011",
]
